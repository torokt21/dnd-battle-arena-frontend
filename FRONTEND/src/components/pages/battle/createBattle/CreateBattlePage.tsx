import { Container, Typography } from "@mui/material";

export default function CreateBattlePage() {
	return (
		<Container>
			<Typography variant="h4">Battle Setup</Typography>
		</Container>
	);
}
